/** @type {import('tailwindcss').Config} */
export const content = ["./src/**/*.{html,js}"];
export const theme = {
  extend: {
    colors: {
      primary: '#FF0000',
      secondary: '#00FF00',
      // Другие пользовательские цвета здесь
    },
  },
variants: {},
plugins: [],
}